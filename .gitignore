.agents
screenshots
skills
node_modules
dist
*.tsbuildinfo
*.car


# Local env / secrets — the merchant mnemonic and any local overrides.
.env
.env.*
!.env.example

# Local merchant credential bundle + upload receipts — plaintext PEMs and
# (block,index) renewal coordinates. NEVER commit; only the encrypted envelope
# is uploaded. The committed example template carries placeholders only.
.credentials/*.local.json
.credentials/*.receipt.json
.credentials
.DS_Store

# Sentry Config File
.env.sentry-build-plugin

# AI coding-agent session artifacts — never commit these.
# Intentional team config (.claude/commands|skills|agents/ and CLAUDE.md) is
# NOT ignored; add it explicitly if the team adopts it.
.claude/sessions/
.claude/cache/
.claude/settings.local.json
.opencode
qr-codes